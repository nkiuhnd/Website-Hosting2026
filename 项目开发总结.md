# 项目开发总结与经验回顾

本文档梳理了本项目（Website-Hosting）的开发历程、关键技术架构、遇到的主要问题及解决方案，旨在为今后的开发提供经验参考，避免重复踩坑。

## 1. 项目概况与架构

### 1.1 技术栈
- **前端**: React, Tailwind CSS, Lucide React (图标), qrcode.react (二维码)
- **后端**: Node.js (Express), Prisma ORM (SQLite), JWT Authentication
- **部署**: Docker (Multi-stage build), Nginx (反向代理 - 假设)

### 1.2 核心功能
- 用户/项目管理（增删改查）
- 静态网页托管与上传
- 个人中心（仪表盘、项目统计、访问日志）
- 管理员后台（违规举报处理、用户管理）
- 安全机制（静态页防本地运行、JWT鉴权）

---

## 2. 开发历程与问题解决 (按模块梳理)

### 2.1 基础设施与 Docker 环境

**问题 1: Docker 容器内找不到客户端目录 / 挂载问题**
- **现象**: 部署时提示文件或目录不存在，或者构建体积过大。
- **原因**: 路径映射错误，以及构建阶段未分离。
- **解决方案**:
    - 采用 **Multi-stage builds (多阶段构建)**：分离 `builder` 和 `production` 阶段，仅保留 `dist` 和 `node_modules` (production)，显著减小镜像体积。
    - 明确 `WORKDIR` 和 `COPY` 指令，确保目录结构清晰。
    - 升级 Node.js 版本至 `v20.19.4` 以获得更好的性能和安全支持。
    - **经验**: Dockerfile 编写应始终遵循“最小化镜像”原则，且路径尽量使用绝对路径或清晰的相对路径。

**问题 2: 数据库文件挂载错误**
- **现象**: Docker 启动时报错 "not a directory" 或数据丢失。
- **原因**: 宿主机挂载点如果不存在，Docker 可能会将其自动创建为目录而不是文件。
- **解决方案**: 确保宿主机上的 SQLite 文件 (`production.db`) 在启动容器前已存在。如果不存在，应先创建一个空文件。

### 2.2 后端 API 与 数据安全

**问题 3: 管理员删除用户失败**
- **现象**: 删除用户时报错，因为该用户下还有关联的项目文件或数据库记录。
- **原因**: 外键约束导致无法直接删除用户；文件系统中的用户目录未清理。
- **解决方案**:
    - **级联删除逻辑**: 在删除用户前，显式删除其所有关联项目 (`prisma.project.deleteMany`)。
    - **文件清理**: 使用 `fs.remove(user.storagePath)` 彻底清除用户上传的物理文件。
    - **代码参考**: `server/src/routes/admin.ts` 中的 `DELETE /users/:id` 接口。

**问题 4: 静态网页被本地非法运行**
- **现象**: 用户下载网页后直接在本地浏览器打开，绕过平台验证。
- **解决方案**: **脚本注入 (Script Injection)**。
    - 在用户上传 HTML 文件时，后端自动在 `</body>` 前注入一段 JS 代码。
    - 检测 `window.location.protocol === 'file:'`，如果是则清空页面并抛出错误。
    - **经验**: 对于托管类项目，可以通过“中间人”处理（上传时修改内容）来增强安全性。

### 2.3 前端 UI 与 交互体验

**问题 5: 个人中心 (Profile) 界面陈旧且功能单一**
- **需求**: 需要现代化的仪表盘，展示项目统计而非简单的登录日志。
- **解决方案**:
    - **UI 重构**: 采用卡片式布局 (Card Layout)，使用 Tailwind CSS 实现响应式设计。
    - **数据聚合**: 新增 API 聚合数据（项目数、占用空间、总访问量）。
    - **列表优化**: 将“最近登录”改为“项目列表”，并增加**排序功能** (按名称、空间、访问量排序)。
    - **细节优化**: 针对长链接使用 CSS `truncate` 防止破坏布局，去除横向滚动条。

**问题 6: 移动端/家长端分享不便**
- **解决方案**: 引入 **QR Code (二维码)** 功能。
    - 使用 `qrcode.react` 组件。
    - 在上传成功后及项目列表中提供“分享”按钮，弹出模态框展示大图二维码，方便微信扫描。

**问题 7: `ReferenceError: Smartphone is not defined`**
- **原因**: 复制粘贴代码时遗漏了图标组件的导入。
- **解决方案**: 补充 `import { Smartphone } from 'lucide-react';`。
- **经验**: 在重构或复制代码时，务必检查 IDE 的 import 提示，或运行前进行静态检查。

---

## 3. 避坑指南与最佳实践 (Lessons Learned)

### 3.1 数据库与 Prisma
- **Schema 设计**: 在设计数据库时，务必考虑“级联删除” (Cascade Delete) 的需求。如果数据库层不支持（如某些 SQLite 配置），必须在应用层（代码中）手动处理关联数据的删除顺序（先子后父）。
- **类型安全**: 在 Express 路由中使用 `req.params` 时，注意 TypeScript 类型推断。必要时显式转换类型（如 `parseInt(req.params.id)`），避免类型不匹配导致的运行时错误。

### 3.2 前端开发
- **组件复用**: 像“模态框 (Modal)”、“二维码分享”这样的功能，应尽早封装为独立组件 (`ShareQRCodeModal`), 避免代码重复。
- **布局溢出**: 在设计表格或列表时，始终对文本类内容（URL、描述）设置最大宽度 (`max-w-xs`) 和截断 (`truncate`)，防止内容撑开容器导致布局错位。

### 3.3 部署运维
- **配置分离**: 环境变量（如 `JWT_SECRET`, `UPLOAD_DIR`）应严格通过 `.env` 文件管理，禁止硬编码在代码中。
- **备份策略**: 生产环境更新 Docker 镜像前，务必对当前 `latest` 镜像进行 Retag 备份（如 `v1.0-backup`），以便快速回滚。

## 4. 结语

本项目经过多次迭代，已从基础的托管功能演进为具备完善管理后台、用户分析和安全保护的现代化平台。后续开发建议继续遵循**“组件化前端”**和**“服务层后端”**的模式，保持代码的清晰与可维护性。

还有个问题没有说明。
在本地构建镜像，上传到 Docker Hub 后，如何在服务器上拉取最新镜像。不要再服务器上编译，在本地编译完成，上传到 Docker Hub 后，在服务器上拉取最新镜像。
- **解决方案**: 在服务器上执行 `docker pull hanshuai23/web-hosting-platform:latest`。
- **经验**: 为了避免手动操作，建议在 `docker-compose.yml` 中指定 `image: hanshuai23/web-hosting-platform:latest`，并在部署时执行 `docker compose up -d`。